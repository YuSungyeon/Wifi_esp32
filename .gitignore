# --- OS ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# --- 로컬 실행 로그 (수집 reader 로그 등) ---
log/

# --- Editors / IDE ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- ESP-IDF / CMake ---
# esp-idf/ → git submodule (버전은 .gitmodules + 커밋으로 고정)
.espressif/
build/
**/build/
sdkconfig.old
sdkconfig.defaults.old
dependencies.lock
managed_components/

# --- Python (도구·스크립트 공통) ---
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
*.egg-info/
.eggs/
dist/
*.egg

# --- 로그·임시 ---
*.log
*.tmp
*.temp

# --- Microsoft Office (발표·문서 바이너리) ---
*.pptx
*.ppt
*.docx
*.doc
*.pdf
~$*

# --- 수집 데이터 (로컬 실험 산출물) ---
mac_collector_output/
mac_collector/flash_state.json

# --- 전처리 산출물 (X/y npy, manifest — 세션당 수백 MB) ---
model_train/preprocessing/output/

# --- 모델 학습 산출물 (checkpoint, 지표, 예측) ---
model_train/lstm/runs/

# --- 로컬 질의응답 기록 (Git에 올리지 않음) ---
doc/qa-log.md

# --- 발표·보고 (presentations/로 이동) ---
presentations/

/docs/

# clangd 인덱스 캐시
**/.cache/
